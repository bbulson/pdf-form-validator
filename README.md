# pdf-form-validator
Java-based automation suite using Apache PDFBox to streamline data injection and visual validation. The tool automatically maps data to every form field and generates annotated reports with directional pointers to audit mapping accuracy. It automatically highlights and points to fields to ensure 100% data accuracy.
